import 'package:flutter/material.dart';


class ProductList extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    // Here we cont need Scafold as we are not creating a page we are just embeding this view with parent view
    return Center(child: Text("Product List Page"),);
  }
}